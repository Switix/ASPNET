@model IEnumerable<Kategoria>
@{
    foreach(var kategoria in Model)
    {
        <a style="color:white" href="@Url.Action("Lista","Filmy",new{nazwaKategorii = kategoria.Nazwa})">@kategoria.Nazwa</a>
    }
    <a class="btn btn-success" href="@Url.Action("Index","Koszyk")">koszyk</a>
}
